# Copy to .env if you want local Vite builds to hit the deployed backend.
# (Vercel should be configured via Environment Variables, not committed .env)

VITE_API_URL=https://weather-intelligence-hub.onrender.com/api

